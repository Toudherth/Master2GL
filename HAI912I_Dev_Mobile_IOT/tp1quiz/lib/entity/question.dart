class Question {
  String questionText;
  bool isCorrect;
  String imageUrl; // URL de l'image pour la question

  Question({
    required this.questionText,
    required this.isCorrect,
    required this.imageUrl,
  });
}
